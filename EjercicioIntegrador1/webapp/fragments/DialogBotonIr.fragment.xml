<core:FragmentDefinition xmlns="sap.m" 	xmlns:core="sap.ui.core" xmlns:l="sap.ui.layout">

  
        <Dialog>
        <l:VerticalLayout class="sapUiContentPadding" width="100%">
        <Toolbar>
        
              <Title text="{i18n>titleDialogIngreso}" titleStyle="H4" />
        
        </Toolbar>
        <Label text="{i18n>fechaIngresada}"></Label>

        <Text text="{modelInfo>/fechaIngresada}"></Text>

        <Label text="{i18n>proveedorSeleccionado}"></Label>
        <Text text="{modelInfo>/proveedor}"></Text>
        <Label text="{i18n>paisIngresado}"></Label>
       
         <Text text="{modelInfo>/paisIngresado}" ></Text>
        <Button text="{i18n>cerrarDialog}" press="onCloseDialog"       ></Button>

        </l:VerticalLayout>
        
        </Dialog>


</core:FragmentDefinition>